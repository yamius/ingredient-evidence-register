# Pinned build dependencies for the Vallydia Ingredient-Evidence Register.
# Reproduce the dataset with:
#   python -m pip install -r build/requirements.txt
#   python build/enrich_identifiers.py     # chemical identifiers (PubChem; cached)
#   python build/generate_images.py        # molecular structures (RDKit)
#   python build/generate_dataset.py       # all data artifacts + checksums

python-frontmatter==1.3.0
PyYAML==6.0.3
pandas==3.0.3
pyarrow==25.0.0
rdkit==2026.3.3
